Semaine pro (lundi 26 juillet - 20h):

1- Commentaires :

Is validated = true only when admin validates the comment
Default => is validated is false
Show = only validated comments

2- Page acceuil :
+ formualaire contact !!!!

3- Page admin :
Commencer : 2 onglets (comments + posts)

Lien github : https://github.com/MedBenYsf/php-mvc-twig

Obj session 19 septembre :

1. switcher sur develop et vérifier que ça contient tout (sinon git merge) // NE PAS FAIRE DE MERGE REQUEST
2. Regarder htaccess => MAMP => redirections plus propres + regarder la sécurité (ex : failles XSS, injections SQL)
3. travailler truc de sécurité sur develop





        Session::put('foo', 'bar');
        $bar = Session::get('foo');
        Session::forget('foo');


Session 19 septembre : 

Virtual host => pour faire sa propre url
